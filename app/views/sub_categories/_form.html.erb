<%= form_for(@sub_category) do |f| %>
  <% if @sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
      <% @sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    
    
<%= select_tag("sub_category[category_id]",  options_for_select(@category.map{|c|["#{c.descripcion}", c.id]})) %>
   

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script type="text/javascript">
          $(document).ready(function() {
            console.log("hola")
          $('select#sub_category_category_id').select2({
            placeholder: "Categoria",
            allowClear: true
          });
        });
</script>